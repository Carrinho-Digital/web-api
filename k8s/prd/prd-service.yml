apiVersion: v1
kind: Service
metadata:
  name: prd-carrinhodigital-service
spec:
  type: ClusterIP
  selector:
    app: carrinhodigital-webapi
    env: prd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
